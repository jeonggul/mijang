package com.example.mijang.config;

import org.springframework.context.annotation.Configuration;

/**
 * MyBatis 추가 설정.
 *
 * <p>개발명세서(MVC) · 공통/설정 · Common.config
 * <p>TODO: mapper-locations·map-underscore-to-camel-case 는 application.properties 에 있다.
 * <p>TODO: common.type 의 TypeHandler 를 등록하려면 여기서 ConfigurationCustomizer 를 쓴다.
 */
@Configuration
public class MyBatisConfig {
}
